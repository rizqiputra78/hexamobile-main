import 'package:flutter/material.dart';

class AnimeFavorit extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
        appBar: AppBar(
            title: Text("ini halaman favorite")
        ),
        body: Center(
         
        )
    );
  }
}