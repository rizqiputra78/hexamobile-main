import 'package:flutter/material.dart';

class InfoAnime extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
        appBar: AppBar(
            title: Text("ini halaman Parfume")
        ),
        body: Center(
         
        )
    );
  }
}